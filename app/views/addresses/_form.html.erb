<%= form_with(model: setup_address(@address), local: true) do |form| %>
  <% if address.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(address.errors.count, "error") %> prohibited this address from being saved:</h2>

      <ul>
      <% address.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :lastName %>
    <%= form.text_field :lastName, id: :address_lastName %>
  </div>

  <div class="field">
    <%= form.label :firstName %>
    <%= form.text_field :firstName, id: :address_firstName %>
  </div>

  <div class="field">
    <%= form.label :street1 %>
    <%= form.text_field :street1, id: :address_street1 %>
  </div>

  <div class="field">
    <%= form.label :street2 %>
    <%= form.text_field :street2, id: :address_street2 %>
  </div>

  <div class="field">
    <%= form.label :city %>
    <%= form.text_field :city, id: :address_city %>
  </div>

  <div class="field">
    <%= form.label :state %>
    <%= form.text_field :state, id: :address_state %>
  </div>

  <div class="field">
    <%= form.label :zip %>
    <%= form.text_field :zip, id: :address_zip %>
  </div>

Spouse:
  <%= form.fields_for :spouse, :child_index =>  ->{ (Time.now.to_f * 1000000).to_i  } do |ff| %>
    <div class="field">
      <%= ff.text_field :firstName %>
      <%= ff.text_field :lastName %>
    </div>
  <% end %>

Children:
<table id="children_table" class="children_table">
  <thead><tr><th id="column_0">First Name</th><th id="column_1"></th></tr></thead>
  <%= form.fields_for :children, :child_index =>  ->{ (Time.now.to_f * 1000000).to_i  } do |ff| %>
      <tr id="<%= h("child_#{ff.object.id}") %>" class="person">
        <td>
        <%= ff.text_field :firstName, :size => 40 %>
        <%= ff.text_field :lastName, :value => @address.lastName, :hidden => true %>
        </td>
        <td>
          <% if (ff.object.persisted?) %>
            Delete
          <% end %>
        </td>
       </tr>
  <% end %>

    <tfoot><div class="links">
      <tr>
        <td>Add Child</td>
      </tr>
    </div></tfoot>
</table>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
