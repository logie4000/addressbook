<p id="notice"><%= notice %></p>

<h1>Addresses</h1>

<table>
  <thead>
    <tr>
    </tr>
  </thead>

  <tbody>
    <% row_id = 0 %>
    <% @addresses.each do |address| %>
      <div id="addressee_<%= address.addressee.id %>">
        <%= render :partial => "people/person_row", :locals => {:person => address.addressee} %>
      </div>
      <% row_id += 1 %>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Address', new_person_path %>
